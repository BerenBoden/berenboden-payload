import { CollectionConfig } from "payload/types";

const Resources: CollectionConfig = {
  slug: "resources",
  fields: [
    {
      name: "myField",
      type: "text",
    },
  ],
};

export default Resources;
